import 'package:flutter/material.dart';

class CopyFormPage extends StatelessWidget {
  const CopyFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}